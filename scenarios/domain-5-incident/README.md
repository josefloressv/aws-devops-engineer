# Domain 5 — Incident and Event Response (14% of exam)

Priority: **high** (needs improvement on the last attempt).

Typical topics: EventBridge rules/targets/dead-letter queues, SNS/SQS event-driven
remediation, Systems Manager Automation documents, Lambda-based auto-remediation,
AWS Health/Personal Health Dashboard integration, incident escalation patterns.

Each scenario lives in its own subfolder here:
`scenarios/domain-5-incident/<scenario-slug>/{template.yaml,params.json,README.md}`.
Scaffold a new one with `make new DOMAIN=domain-5-incident NAME=<scenario-slug>`.
